# Repository agent instructions

## Git naming

- Never create or switch to a branch whose name starts with `codex/`.
- Never include `codex/` in a commit message.
- Keep working on the current branch unless the user explicitly requests another branch.
- If a new branch is required and the user has not supplied its name, ask for the branch name before creating it.
